# Postgres connection settings
POSTGRES_DB=infocus
POSTGRES_USER=infocus
POSTGRES_PASSWORD=infocus
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Optional: change to a persistent volume path if needed
# PGDATA=/var/lib/postgresql/data/pgdata
